const PaymentResponse = ({
  id,
  totalAmount,
  paidAt,
  note,
  createdAt,
  updatedAt,
  minSpentAt,
  maxSpentAt,
  expenseCount,
}) => ({
  id,
  totalAmount,
  paidAt,
  note,
  createdAt,
  updatedAt,
  minSpentAt,
  maxSpentAt,
  expenseCount,
});

export default PaymentResponse;
