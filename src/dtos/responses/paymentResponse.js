export const PaymentBySelectedExpenseResponse = ({
  id,
  totalAmount,
  paidAt,
  note,
  createdAt,
  updatedAt,
}) => ({
  id,
  totalAmount,
  paidAt,
  note,
  createdAt,
  updatedAt,
});

export default PaymentBySelectedExpenseResponse;
