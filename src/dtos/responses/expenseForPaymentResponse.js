export const ExpenseForPaymentResponse = ({
  id,
  title,
  amount,
  finalAmount,
  category,
  isSplitBill,
  spentAt,
  createdAt,
  updatedAt,
}) => ({
  id,
  title,
  amount,
  finalAmount,
  category,
  isSplitBill,
  spentAt,
  createdAt,
  updatedAt,
});

export default ExpenseForPaymentResponse;
