const ExpenseRequest = ({title, amount, category, note, isSplitBill, paidAt, spentAt}) => ({
    title,
    amount,
    category,
    note,
    isSplitBill,
    paidAt, spentAt
});

export default ExpenseRequest;